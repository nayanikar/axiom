# Copy to backend/.env — never commit real secrets
ANTHROPIC_API_KEY=

# Match prepared Intent Space (see AXIOM_DESIGN.md)
SPACE_ID=space-6f9708cd-d6f0-4ac5-9c1b-9c6527b37a2a
AGENT_NAME=todd

# Optional: SQLite URL (defaults to backend/swarmlearn.db)
# DATABASE_URL=sqlite+aiosqlite:///.../axiom.db

# Optional: Spacebase station origin
# SPACEBASE_ORIGIN=https://spacebase1.differ.ac

# Claim Spacebase1 via app UI: paste full claim URL; do not store claim token in tracked files.
